from distutils.core import setup

setup(name = 'bind',
      version = '1.0',
      description = 'Argument binding for Python',
      author = 'Eric Kern',
      author_email = '',
      url = '',
      packages = ['bind']
 )

